If (Form event code:C388=On Data Change:K2:15)
	Self:C308->:=Uppercase:C13(Self:C308->)
	//checkInit 
	//c_text($UID)
	//$UID:=makeBranchUID
	//checkifRecordExists (->[Branches];->[Branches]UID;->$UID)
	//If (isValidationConfirmed )
	//Else 
	//beep
	//Self->:=""
	//end if
	
End if 