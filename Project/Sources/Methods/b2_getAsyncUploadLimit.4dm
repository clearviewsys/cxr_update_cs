//%attributes = {}
// returns the limit in size for async upload
// if file is bigger than this, it will be uploaded asynchronically
// put in key values in future

C_LONGINT:C283($0)

//$0:=1024*1024*100  // 100 Mbytes
$0:=1024*1024*50  // 50 Mbytes
