//%attributes = {}
// prefix just like webB except for links

// webB is for methods that are called from an HTML FORM

// webL is for methods that are called from and HTML Link (but carry the context ID and/or some other stuff)

