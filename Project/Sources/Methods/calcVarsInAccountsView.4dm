//%attributes = {}
// calcVarsInAccountsView
// this method is called from inside the form method of [ACCOUNTS].forms.view

C_REAL:C285(vSumDebits; vSumCredits; vBalance)

getRegisterSums(->vSumDebits; ->vSumCredits; ->vBalance)