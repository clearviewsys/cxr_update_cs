//%attributes = {}
// get is a prefix that should be used to retreive multiple values through passing pointers

// it is an accessor prefix


// calc is a prefix that should be used when a calculation is involved returning a single value

// if more than one values are returned in passed pointers (variables) then use the 'get' prefix


// ex: calcAverage (->array) :real

// ex : getStatVars (->array; ->mean1;->stdvar; ->productvar).