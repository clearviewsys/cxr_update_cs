//%attributes = {}
// _UNIT_TEST
// testing handleDD_CustomersAMLfilters
// this template needs to be completed to set an example on how to test dropdown methods

C_OBJECT:C1216($test)
$test:=AJ_UnitTest.new("handleDD_CustomersAMLFilters"; Current method name:C684; "Controllers.DropDown")

// test the on load

// test the on clicked

// each row must be 
