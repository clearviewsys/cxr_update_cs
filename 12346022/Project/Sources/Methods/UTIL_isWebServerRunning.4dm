//%attributes = {"executedOnServer":true}
//this executes on server so can be called by client and will know if 
//web server is running at the server level

C_BOOLEAN:C305($0)


$0:=WEB Is server running:C1313