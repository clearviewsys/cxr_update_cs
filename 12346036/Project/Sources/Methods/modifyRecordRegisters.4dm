//%attributes = {}
//CONFIRM("It is advised to use the invoice (or source tables) to modify entries in the jour"+"nal registry.";"Back to form";"Modify")
//If (OK=0)
modifyRecord(->[Registers:10])
//End if 