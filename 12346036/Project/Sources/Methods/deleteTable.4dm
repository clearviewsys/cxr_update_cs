//%attributes = {}
// use delete for cleanup after any deletions

// fgor example if a one record is deleted then the many records must be deleted as well

// for example in the case of customers and picture IDs